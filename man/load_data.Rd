% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_data}
\alias{load_data}
\title{Load and preprocess count data for NMF}
\usage{
load_data(counts, filter_th = 10, tau = 0.5, S_th = 0.001)
}
\arguments{
\item{counts}{Numeric matrix of counts, where rows are features (e.g., genes)
and columns are observations (e.g., spots or cells). Column names should
encode positions in the form "x\if{html}{\out{<coord>}}x\if{html}{\out{<coord>}}".}

\item{filter_th}{Numeric, minimum sum per row to keep a feature (default 10).
Features with row sums ≤ \code{filter_th} are removed.}

\item{tau}{Numeric, target mean value for optimization of gamma (default 0.5).
This is used in computing the spatial similarity matrix.}

\item{S_th}{Numeric, threshold below which entries of the similarity matrix
are set to zero (default 1e-3).}
}
\value{
A list with two elements:
\describe{
\item{counts}{Filtered and converted numeric matrix of counts.}
\item{S}{Normalized spatial similarity matrix.}
}
}
\description{
This function filters and preprocesses a count matrix for non-negative
matrix factorization (NMF), and computes a spatial similarity matrix \code{S}
based on the positions of the columns.
}
\details{
The function performs the following steps:
\enumerate{
\item Filters rows of the counts matrix with low total counts.
\item Extracts x and y positions from column names.
\item Computes a spatial similarity matrix \code{S} where
\eqn{S_{ij} = exp(-\gamma * d_{ij}^2)} for the Euclidean distance
between positions. Small values below \code{S_th} are set to zero.
\item Normalizes \code{S} so that row sums equal 1.
\item Optimizes the parameter \code{gamma} to minimize \eqn{(mean(diag(S)) - \tau)^2}.
}
}
\examples{
\dontrun{
# Suppose counts is a numeric matrix with column names like "10x15"
result <- load_data(counts, filter_th = 5, tau = 0.4)
counts_filtered <- result$counts
S_matrix <- result$S
}

}
