# GPUmatrix supplies S4 generics for these base-like operations. Import them
# explicitly so installed-package dispatch matches scripts using library(GPUmatrix).
# Keep this list in sync with NAMESPACE when regenerating with roxygen2.
#' @importFrom GPUmatrix as.matrix colSums mean ncol nrow rowSums t
NULL
